name: Test Action

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      # Add test steps here to verify the action works
      - name: Build Docker image
        run: docker build -t github-team-validator .
      
      # Add more tests as needed
